// iterables

/**
 * jispr hum for of loop lga sak te h 
 * string, array are iterables
 */

const firstN = "Aligator";
for (let char of firstN) {
    // console.log(char)
}

// arry like object
/**
 * jispr pas lenght property hoti h 
 * aur jinko hum index se access kr sakte h 
 * e.g :- string
 */
const usr = ["key1", "key2", "key3", "key4"];

for (user of usr) {
    // console.log(user)
}