/** Array puush pop
 * Array shift unshift
 * */

let fruits = ["apple", "banana", "gamer" ]

/**  { Push and Pop } Method
 * Both are fast method
 * Push() method will push an element at the end of the array
 * Pop() method will remove an element at the end of the array and return as value 
 */ 




// console.log(fruits.push("Eat"))
// console.log(fruits)

// Pop method
// this method can remove last element of the array
//  and return removed element as
// string, integer, null, obj, undefined
// what ever is in your array list
// let pushed = []; // empty array

// p =  pushed.push(fruits.pop()); // this will store and retur lenght of the poped element

// console.log(pushed) // showing value what inside  in console

/** { Unshift and shift } method
 *  both are slow  methods 
 * Unshift() method will push an element at start of the an array
 * shift() method will remove an element at start of the an array and return removed element
 */



let unshiftF = ["tiger"];

UN = unshiftF.unshift("Lion", "Hyena", "Snow Tiger")

console.log(unshiftF)